# Restructure: money scope + process-flow audit (2026-06-03)

Founder reset the monetization scope mid-OT-12-testing. This doc locks the new
scope, audits the full booking→payment flow against it, lists the interconnected
problems/blockers, and breaks the restructure into slices. Companion to
`docs/roadmap.md` (§3) and `DECISIONS.md`.

---

## 1. Locked scope (2026-06-03)

1. **The booking process is universal and free of any payment setup — no tradeoffs.** Intake, organization, calendar, waitlist, flash, guest spots all work with zero Stripe. This is the part every artist gets.
2. **Deposit collection is an optional opt-in.** Only artists who want the comfort of collecting deposits in-app turn it on.
3. **All deposit money flows through Inklee, and Inklee takes a percentage platform fee** (covers Stripe fees + margin) → a **transaction-fee revenue stream** alongside subscriptions. _Fee mechanic resolved 2026-06-03 → **deducted, 3% all-in**: the customer pays exactly the deposit; the artist loses ~3% total, with Inklee absorbing Stripe's standard processing fee out of that 3% (artist stays MoR). See §5 D-a/D-b._
4. **Artists stay merchant of record** (Stripe Connect destination charge: `on_behalf_of` + `transfer_data.destination` + **new** `application_fee_amount`). Inklee never holds artist money beyond the platform fee.
5. **Goods are showcase-only** — products/variants display on the bio page; **no in-app checkout, no appointment add-ons**.
6. **Connect onboarding is opt-in, minimized, and clearly framed** ("Stripe verifies you so deposits land in _your_ account; Inklee never holds your money").

**Why this is coherent:** the friction (Stripe KYC) is now strictly opt-in and only borne by artists who _choose_ in-app collection; everyone else is unaffected. The fee is what makes the optional feature a real, profitable use case. Artist-as-MoR (LO-2, counsel-cleared) is preserved — `application_fee` rides on top of the existing destination-charge design.

---

## 2. Process-flow audit (current → required)

| Stage                                                                                                               | Today                                                                                                                                 | Required under new scope                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **1. Public booking submit** (`/[slug]` → `booking_requests` pending)                                               | No money. Works without Stripe.                                                                                                       | ✅ No change. This is the universal core.                                                                                  |
| **2. Artist reviews request** (dashboard)                                                                           | No money.                                                                                                                             | ✅ No change.                                                                                                              |
| **3. [opt] Request deposit** (`requestDeposit`, `bookings/actions.ts:529`)                                          | Connected artist → destination charge, **no fee**. **Un-connected artist → platform charge (money lands in Inklee's account).**       | **Gate on active Connect** (block/hide for un-connected). **Add `application_fee_amount`.**                                |
| **4. Customer pays** (`/request/[token]` portal + `addons-checkout.tsx` + Stripe Elements `confirmPayment`)         | Deposit **+ goods add-ons** combined; `prepareCheckoutAction` recomputes total, builds `orders`/`order_items`, updates intent amount. | **Remove the add-on checkout.** Deposit-only payment. **Show Inklee's fee transparently** (Deposit + Service fee = Total). |
| **5. Webhook** `payment_intent.succeeded`                                                                           | Marks deposit paid, booking approved, **creates goods order + decrements inventory + itemized email**.                                | Deposit-only confirmation. Orders simplify to deposit/fee records (or drop).                                               |
| **6. Goods module** (CRUD + bio cards + `is_checkout_addon` + `booking_interests` + Accept-time availability popup) | Full commerce: cart at booking, confirm-availability on Accept, payable at checkout.                                                  | **Showcase only** — keep product/variant display on the bio page; remove cart/interests/checkout/orders-product-lines.     |
| **7. Connect onboarding** (`/settings/payouts`)                                                                     | Express, US-sandbox-flavored in test, full upfront.                                                                                   | Opt-in framing, artist-country (EU), progressive/minimal onboarding, "never holds your money" copy.                        |

---

## 3. Interconnected problems & blockers

- **B1 — Deposit collection isn't gated on Connect (blocker + live exposure).** `requestDeposit` proceeds for un-connected artists and routes the deposit to **Inklee's** platform account → Inklee becomes MoR + holds funds, the exact thing to eliminate. Fix: hard-gate deposit collection on `routeCharges` (active Connect + charges_enabled), server-side and in every deposit UI surface (dashboard, booking detail, `/bookings/deposits`).
- **B2 — No platform fee (blocker).** `application_fee_amount` does not exist anywhere. Add it to the deposit PaymentIntent. Decide **fee model** (D-a) + **percentage** (D-b).
- **B3 — Fee transparency (legal/UX blocker).** Customer must see Inklee's fee before paying (EU consumer/surcharge transparency). Artist must see "you receive X, Inklee fee Y." Counsel item.
- **B4 — Refunds with fee + Connect.** On cancellation/refund of a deposit: refund the application fee? Who eats Stripe's processing fee? Define + implement (check current refund support — appears absent).
- **B5 — VAT on Inklee's fee.** The platform fee is Inklee revenue → VAT + invoicing (ties to LO-7 Estonian VAT threshold). Counsel item.
- **B6 — Goods-removal blast radius.** `booking_interests` (cart), `addon-products.ts`, `orders`/`order_items` product lines, the Accept-time availability popup, `prepareCheckoutAction` goods path, `CHECKOUT_ADDONS_PROD_READY`. Decide **park vs remove** (D-c). Keep product/variant display for showcase.
- **B7 — Business-model interaction.** Transaction fee vs subscription (Solo Plus). Does the deposit fee apply on all tiers? Is it the free-tier monetization, reduced/removed on paid tiers? (D-d) — interconnected with BM phases in `business-model.md`.
- **B8 — Marketing reframe.** `/tattoo-deposit-tool` + homepage/SEO "collect deposits" → optional feature + fee disclosure.
- **B9 — Onboarding country/UX.** Create Connect accounts in the artist's country (not US default); reduce upfront fields; opt-in framing.
- **B10 — OT-12.3 cutover + runbook now wrong.** `CHECKOUT_ADDONS_PROD_READY` + goods E2E become moot. OT-12 reduces to deposit-Connect + application fee. `docs/ot-12-rollout-runbook.md` needs a rewrite.
- **B11 — Phase D walkthrough** must re-cover the revised deposit + fee flow.

---

## 4. Restructure slices

| Slice    | Title                                            | Closes     | Risk        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------- | ------------------------------------------------ | ---------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **RS-1** | Scope lockdown + docs/memory                     | —          | Low         | This doc + roadmap/DECISIONS/memory. **Done in this pass.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **RS-2** | Gate in-app deposit collection on active Connect | B1         | Low         | ✅ **Done 2026-06-03 (`02efea8`).** Refined: instead of _blocking_ un-connected artists, `requestDeposit` only creates a PaymentIntent when `routeCharges`; un-connected artists get a **manual deposit** (no intent — client pays directly per the note, artist marks received; portal already degraded to this). MoR exposure closed; deposits stay usable for everyone. Booking detail passes `canCollectInApp`; deposit form explains manual mode + nudges Connect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **RS-3** | Goods → showcase                                 | B6         | Medium      | ✅ **Done 2026-06-03.** Parked behind one master switch `isGoodsCommerceEnabled()` (env `GOODS_COMMERCE_ENABLED`, default OFF — D-c = park, not delete). Gates: both data sources (`getInterestEligibleProducts` / `getAddonProducts` → `[]`), the public `interestEligible` flag (Shop overlay degrades to a showcase gallery), the `[slug]` interest insert, and the portal add-on computation. Customer portal now renders the deposit-only `DepositPaymentForm` (no `prepareCheckoutAction` round-trip). **F5 fixed in the same slice** — `requestDeposit` reuse path now syncs the Stripe intent amount + resets metadata. Accept popup + webhook goods fan-out left dormant (parked, no rows ever created). Closes audit F2, F5, F11.                                                                                                                                                                                                                                                                                |
| **RS-4** | Platform fee (`application_fee_amount`)          | B2, B3     | Medium      | ✅ **Done 2026-06-03.** Flat **3% all-in**, deducted, with Inklee absorbing Stripe's standard processing fee (fee-bearer call 2026-06-03 = keep artist as MoR). Single-source `src/lib/platform-fee.ts`: `platformFeeCents`/`platformFeeEur`/`artistNetEur` = the artist's headline 3% (display); **`applicationFeeCents` = what Inklee actually KEEPS = `max(0, 3% − (1.5% + €0.25))`** = the Stripe `application_fee_amount`. `requestDeposit` sets it on intent create **and** reuse. `on_behalf_of` stays (artist MoR, LO-2 intact) → Stripe debits its fee from the artist; the application fee is sized so the artist's all-in loss is ~3% on a standard EU card (foreign/premium cards cost the artist a little more; deposits < ~€17 clamp Inklee's fee to 0). Deposit form shows "Inklee fee (3%, incl. card processing): −€Y · You receive €Z" (in-app only). Webhook logs Inklee's kept `application_fee_eur`. No customer fee line; **F6 moot** (intent amount = deposit). Closes audit F1 + F4. 13 fee tests. |
| **RS-5** | Onboarding friction + framing                    | B9         | Low         | ✅ **Done 2026-06-03.** **F9** country: new `src/lib/connect-countries.ts` (EEA + common, default DE) + a country select on `/settings/payouts` (new accounts only); `startConnectOnboardingAction` validates + passes it to `ensureConnectAccount` so the Express account is created in the artist's country, not the US-default platform country. **F3** `/bookings/deposits` (the real page; `/settings/deposits` redirects there): replaced the false "payments process through Inklee's account / funds settle to the operator" copy + global-Stripe-mode status with a Connect-aware `DepositCollectionStatus` (in-app on/off, deposit lands in the artist's own account, Inklee keeps 3%, manual fallback + Connect link). **F12/F14/F13 copy**: payouts page reframed opt-in, drops goods + the "flip the live switch" footer, adds the 3% fee. Progressive onboarding is Stripe-Express-hosted (out of our control), not actioned.                                                                                |
| **RS-6** | Refunds with fee + Connect                       | B4         | Medium      | ✅ **Done 2026-06-03.** **F10** in-app refunds: new `refundDeposit` action (full refund only v1) — Stripe `refunds.create({ payment_intent, reverse_transfer: true, refund_application_fee: true })` with idempotency key, audit-log state (no migration), double-refund guard. **Policy:** money pulled back from the artist (MoR holder), **Inklee returns its fee** (no profit on a refund), Stripe's processing fee non-refundable → falls on the artist (= same as a dashboard refund). `DepositRefundButton` (confirm flow) on the booking detail's deposit card, shown only for a paid in-app deposit; "Refunded €X" state after. Manual deposits aren't refundable in-app (artist returns the money directly). **F7** dual-confirm: `markDepositReceived` now cancels the live PaymentIntent on manual mark; `StatusActions` gets `hasDepositIntent` → in-app deposits show "Waiting for card payment" + demote the manual mark to an override. Closes audit F7, F10.                                              |
| **RS-7** | Marketing + legal reframe                        | B3, B5, B8 | Low/Counsel | **Marketing copy ✅ Done 2026-06-03** — homepage "Deposit collection" feature card + "Can artists collect deposits" FAQ, and `/tattoo-deposit-tool` (how-it-works intro + "separate payment tool" FAQ) all reframed: deposit collection is **optional**, the deposit **lands in the artist's own Stripe account**, **Inklee keeps 3%**, manual fallback noted. **Legal half = counsel-gated (D-e), NOT done unilaterally** — prepared `docs/payment-flow-for-counsel.md` (full platform/money-flow brief + 8 questions: fee VAT/threshold/invoicing, client vs B2B disclosure, artist disclosure wording, refund-of-fee, MoR-with-fee re-confirm, PSD2/payment-intermediary status, terms clause, VAT on the deposit). Terms clause + privacy/fee wording await counsel.                                                                                                                                                                                                                                                   |
| **RS-8** | OT-12.3 + runbook rewrite + Phase D re-audit     | B10, B11   | Low         | Replace goods-gate cutover with deposit-fee cutover.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

**Sequencing:** RS-1 → **RS-2** (kill live exposure) → **RS-3** (decouple goods) → **RS-4** (fee) → RS-5 → RS-6 → RS-7 (counsel) → RS-8.

---

## 5. Open decisions (need founder / counsel)

- **D-a — Fee model:** ✅ **RESOLVED 2026-06-03 → "deducted"** (founder reversed the earlier on-top lean). Customer pays exactly the deposit `D`; artist receives `D − F`. → mechanics: intent `amount = D` (unchanged from today), `application_fee_amount = F`, `transfer_data.destination` settles `D − F` to the artist; Inklee keeps `F − Stripe fee`. **Consequence for the audit:** the webhook amount check stays deposit-only (intent.amount = D), so **F6 largely dissolves** — only F1 (add `application_fee_amount`) + F4 (show artist net / customer fee disclosure) remain for RS-4. Still need **D-b** (the %).
- **D-b — Percentage (+ fixed?):** ✅ **RESOLVED 2026-06-03 → 3% all-in, no flat component.** Single-sourced in `src/lib/platform-fee.ts` (`PLATFORM_FEE_BPS = 300`). **Fee-bearer call (same day):** the 3% is the artist's _all-in_ deduction and **Inklee absorbs Stripe's standard processing fee** out of it (rather than stacking it on the artist). Mechanically `on_behalf_of` stays (artist = MoR), so the Stripe `application_fee_amount` is set to what Inklee keeps = `3% − standard Stripe fee` (~€2.75 net on a €200 deposit, artist loses ~€6 = 3%). Inklee-as-MoR (which would let the artist lose exactly 3% on every card) was **rejected** to preserve LO-2. Trade-off accepted: foreign/premium cards cost the artist slightly >3%, and deposits < ~€17 clamp Inklee's fee to 0. Revisit alongside D-d (tier interaction).
- **D-c — Goods code:** ✅ **RESOLVED 2026-06-03 → park behind a flag** (`GOODS_COMMERCE_ENABLED`, default OFF). Code retained, not deleted; showcase display stays.
- **D-d — Tier interaction:** does the deposit fee apply on all plans, or is it the free-tier monetization that paid tiers reduce/remove?
- **D-e — Counsel:** fee VAT + invoicing (LO-7), customer fee-disclosure wording, refund-of-fee policy. Reopens nothing on MoR (artist stays MoR) but adds the platform-fee service.

---

## 6. What does NOT change

- The entire non-payment product (booking intake, dashboard, calendar, waitlist, flash, guest spots, bio page) — untouched and remains usable by everyone with no Stripe.
- Artist-as-merchant-of-record (LO-2) — preserved; `application_fee` rides on the existing destination-charge design.
- Subscription monetization (Solo Plus / BM phases) — still valid; the transaction fee is additive, pending D-d.
