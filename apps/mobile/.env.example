# Copy to .env.local (gitignored) and fill in. EXPO_PUBLIC_* are inlined into the
# client bundle at build time — only put PUBLIC values here (anon key, public URL).
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Base URL of the deployed web app that hosts /api/mobile/* (e.g. https://inkl.ee).
# For local dev against `pnpm --filter inklee dev`, use your machine LAN IP, not localhost.
EXPO_PUBLIC_API_URL=https://inkl.ee
