// Re-export shim: canonical source lives in @inklee/shared (packages/shared).
// Kept so existing "@/lib/booking-fsm" imports across apps/web resolve unchanged.
export * from "@inklee/shared/booking-fsm";
