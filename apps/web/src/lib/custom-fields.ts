// Re-export shim: canonical source lives in @inklee/shared (packages/shared).
// Kept so existing "@/lib/custom-fields" imports across apps/web resolve unchanged.
export * from "@inklee/shared/custom-fields";
