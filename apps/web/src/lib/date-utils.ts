// Re-export shim: canonical source lives in @inklee/shared (packages/shared).
// Kept so existing "@/lib/date-utils" imports across apps/web resolve unchanged.
export * from "@inklee/shared/date-utils";
