// Re-export shim: canonical source lives in @inklee/shared (packages/shared).
// Kept so existing "@/lib/entitlements" imports across apps/web resolve unchanged.
export * from "@inklee/shared/entitlements";
