// The branded date picker now lives in the shared DateInput component (used by
// both public booking forms and artist tools). Kept as a re-export so existing
// imports keep working.
export { default } from "@/components/date-input";
